'use client';

const OutreachNote = ({ children }: any) => (
  <p style={{ fontSize: '12px', color: '#555', fontStyle: 'italic', margin: '4px 0 14px', paddingLeft: '4px' }}>{children}</p>
);

export default OutreachNote;
